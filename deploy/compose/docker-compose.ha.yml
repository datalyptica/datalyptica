# Docker Secrets Configuration
secrets:
  postgres_password:
    file: ../../secrets/passwords/postgres_password
  datalyptica_password:
    file: ../../secrets/passwords/datalyptica_password
  postgres_replication_password:
    file: ../../secrets/passwords/postgres_replication_password
  minio_root_password:
    file: ../../secrets/passwords/minio_root_password
  s3_access_key:
    file: ../../secrets/passwords/s3_access_key
  s3_secret_key:
    file: ../../secrets/passwords/s3_secret_key
  grafana_admin_password:
    file: ../../secrets/passwords/grafana_admin_password
  keycloak_admin_password:
    file: ../../secrets/passwords/keycloak_admin_password
  keycloak_db_password:
    file: ../../secrets/passwords/keycloak_db_password
  clickhouse_password:
    file: ../../secrets/passwords/clickhouse_password

services:
  # MinIO Object Storage
  minio:
    image: ghcr.io/datalyptica/datalyptica/minio:${DATALYPTICA_VERSION:-v1.0.0}
    container_name: ${COMPOSE_PROJECT_NAME}-minio
    ports:
      - "${MINIO_API_PORT}:9000" # API
      - "${MINIO_CONSOLE_PORT}:9001" # Console
      - "${MINIO_API_HTTPS_PORT:-9443}:9443" # HTTPS API
    env_file:
      - .env.minio
    environment:
      - MINIO_ROOT_USER=${MINIO_ROOT_USER}
      - MINIO_VOLUMES=/data
      - MINIO_OPTS=--console-address :9001
      - MINIO_REGION=${MINIO_REGION}
      - MINIO_DEFAULT_BUCKETS=${MINIO_BUCKET_NAME}

      # Additional MinIO Configuration
      - MINIO_ADDRESS=${MINIO_ADDRESS}
      - MINIO_CONSOLE_ADDRESS=${MINIO_CONSOLE_ADDRESS}
      - MINIO_VOLUMES=${MINIO_VOLUMES}
      - MINIO_LOG_LEVEL=${MINIO_LOG_LEVEL}
      - MINIO_LOG_FILE=${MINIO_LOG_FILE}
      - MINIO_CACHE_DRIVES=${MINIO_CACHE_DRIVES}
      - MINIO_CACHE_EXPIRY=${MINIO_CACHE_EXPIRY}
      - MINIO_CACHE_QUOTA=${MINIO_CACHE_QUOTA}
      - MINIO_CACHE_EXCLUDE=${MINIO_CACHE_EXCLUDE}
      - MINIO_COMPRESS=${MINIO_COMPRESS}
      - MINIO_COMPRESS_MIME_TYPES=${MINIO_COMPRESS_MIME_TYPES}
      - MINIO_BROWSER=${MINIO_BROWSER}

      # SSL/TLS Configuration
      - MINIO_CERTS_DIR=/certs
      - MINIO_SERVER_URL=${MINIO_SERVER_URL:-https://localhost:9443}
    volumes:
      - minio_data:/data
      - ../secrets/certificates/minio:/certs:ro
      - ../secrets/certificates/ca:/ca:ro
    secrets:
      - minio_root_password
      - s3_access_key
      - s3_secret_key
    networks:
      - storage_network
      - data_network
    restart: unless-stopped
    deploy:
      resources:
        limits:
          cpus: "2.0"
          memory: 2G
        reservations:
          cpus: "1.0"
          memory: 512M
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:9000/minio/health/live"]
      interval: ${HEALTHCHECK_INTERVAL}
      timeout: ${HEALTHCHECK_TIMEOUT}
      retries: ${HEALTHCHECK_RETRIES}
      start_period: ${HEALTHCHECK_START_PERIOD}

  # ==================================================================================
  # CONSENSUS LAYER - etcd Cluster for Patroni (High Availability)
  # ==================================================================================

  # etcd Node 1 (co-located with PostgreSQL Patroni 1)
  etcd-1:
    image: quay.io/coreos/etcd:v3.5.11
    container_name: ${COMPOSE_PROJECT_NAME}-etcd-1
    hostname: etcd-1
    ports:
      - "2379:2379" # Client port
      - "2380:2380" # Peer port
    environment:
      - ETCD_NAME=etcd-1
      - ETCD_INITIAL_ADVERTISE_PEER_URLS=http://etcd-1:2380
      - ETCD_LISTEN_PEER_URLS=http://0.0.0.0:2380
      - ETCD_LISTEN_CLIENT_URLS=http://0.0.0.0:2379
      - ETCD_ADVERTISE_CLIENT_URLS=http://etcd-1:2379
      - ETCD_INITIAL_CLUSTER_TOKEN=etcd-cluster-datalyptica
      - ETCD_INITIAL_CLUSTER=etcd-1=http://etcd-1:2380,etcd-2=http://etcd-2:2380,etcd-3=http://etcd-3:2380
      - ETCD_INITIAL_CLUSTER_STATE=new
      - ETCD_AUTO_COMPACTION_RETENTION=1
      - ETCD_DATA_DIR=/etcd-data
    volumes:
      - etcd1_data:/etcd-data
      - ../secrets/certificates/etcd-1:/etcd-certs:ro
      - ../secrets/certificates/ca:/etcd-ca:ro
    networks:
      - storage_network
      - control_network
    restart: unless-stopped
    deploy:
      resources:
        limits:
          cpus: "0.5"
          memory: 512M
        reservations:
          cpus: "0.25"
          memory: 256M
    healthcheck:
      test: ["CMD", "etcdctl", "endpoint", "health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 30s

  # etcd Node 2 (co-located with PostgreSQL Patroni 2)
  etcd-2:
    image: quay.io/coreos/etcd:v3.5.11
    container_name: ${COMPOSE_PROJECT_NAME}-etcd-2
    hostname: etcd-2
    ports:
      - "23791:2379" # Client port (mapped to avoid conflict)
      - "23801:2380" # Peer port (mapped to avoid conflict)
    environment:
      - ETCD_NAME=etcd-2
      - ETCD_INITIAL_ADVERTISE_PEER_URLS=http://etcd-2:2380
      - ETCD_LISTEN_PEER_URLS=http://0.0.0.0:2380
      - ETCD_LISTEN_CLIENT_URLS=http://0.0.0.0:2379
      - ETCD_ADVERTISE_CLIENT_URLS=http://etcd-2:2379
      - ETCD_INITIAL_CLUSTER_TOKEN=etcd-cluster-datalyptica
      - ETCD_INITIAL_CLUSTER=etcd-1=http://etcd-1:2380,etcd-2=http://etcd-2:2380,etcd-3=http://etcd-3:2380
      - ETCD_INITIAL_CLUSTER_STATE=new
      - ETCD_AUTO_COMPACTION_RETENTION=1
      - ETCD_DATA_DIR=/etcd-data
    volumes:
      - etcd2_data:/etcd-data
      - ../secrets/certificates/etcd-2:/etcd-certs:ro
      - ../secrets/certificates/ca:/etcd-ca:ro
    networks:
      - storage_network
      - control_network
    restart: unless-stopped
    deploy:
      resources:
        limits:
          cpus: "0.5"
          memory: 512M
        reservations:
          cpus: "0.25"
          memory: 256M
    healthcheck:
      test: ["CMD", "etcdctl", "endpoint", "health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 30s

  # etcd Node 3 (Arbiter - standalone for quorum)
  etcd-3:
    image: quay.io/coreos/etcd:v3.5.11
    container_name: ${COMPOSE_PROJECT_NAME}-etcd-3
    hostname: etcd-3
    ports:
      - "23792:2379" # Client port
      - "23802:2380" # Peer port
    environment:
      - ETCD_NAME=etcd-3
      - ETCD_INITIAL_ADVERTISE_PEER_URLS=http://etcd-3:2380
      - ETCD_LISTEN_PEER_URLS=http://0.0.0.0:2380
      - ETCD_LISTEN_CLIENT_URLS=http://0.0.0.0:2379
      - ETCD_ADVERTISE_CLIENT_URLS=http://etcd-3:2379
      - ETCD_INITIAL_CLUSTER_TOKEN=etcd-cluster-datalyptica
      - ETCD_INITIAL_CLUSTER=etcd-1=http://etcd-1:2380,etcd-2=http://etcd-2:2380,etcd-3=http://etcd-3:2380
      - ETCD_INITIAL_CLUSTER_STATE=new
      - ETCD_AUTO_COMPACTION_RETENTION=1
      - ETCD_DATA_DIR=/etcd-data
    volumes:
      - etcd3_data:/etcd-data
      - ../secrets/certificates/etcd-3:/etcd-certs:ro
      - ../secrets/certificates/ca:/etcd-ca:ro
    networks:
      - storage_network
      - control_network
    restart: unless-stopped
    deploy:
      resources:
        limits:
          cpus: "0.5"
          memory: 512M
        reservations:
          cpus: "0.25"
          memory: 256M
    healthcheck:
      test: ["CMD", "etcdctl", "endpoint", "health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 30s

  # ==================================================================================
  # STORAGE LAYER - PostgreSQL with Patroni (High Availability)
  # ==================================================================================

  # PostgreSQL Patroni Node 1 (Primary - elected by Patroni)
  postgresql-patroni-1:
    image: ghcr.io/datalyptica/datalyptica/patroni:${DATALYPTICA_VERSION:-v1.0.0}
    container_name: ${COMPOSE_PROJECT_NAME}-postgresql-patroni-1
    hostname: postgresql-patroni-1
    ports:
      - "${POSTGRES_PORT:-5432}:5432" # PostgreSQL port
      - "8008:8008" # Patroni REST API
    environment:
      # Patroni Configuration
      - PATRONI_NAME=postgresql-patroni-1
      - PATRONI_SCOPE=lakehouse
      - ETCD_HOSTS=etcd-1:2379,etcd-2:2379,etcd-3:2379

      # Patroni API Authentication
      - PATRONI_API_USERNAME=patroni
      - PATRONI_API_PASSWORD=${PATRONI_API_PASSWORD:-patroni123}

      # PostgreSQL Superuser
      - POSTGRESQL_POSTGRES_PASSWORD_FILE=/run/secrets/postgres_password

      # PostgreSQL Application User
      - POSTGRESQL_USERNAME=${POSTGRES_USER:-datalyptica}
      - POSTGRESQL_PASSWORD_FILE=/run/secrets/datalyptica_password

      # PostgreSQL Replication User
      - POSTGRESQL_REPLICATION_USER=replicator
      - POSTGRESQL_REPLICATION_PASSWORD_FILE=/run/secrets/postgres_replication_password

      # PostgreSQL Rewind User (for pg_rewind)
      - POSTGRESQL_REWIND_USER=rewind_user
      - POSTGRESQL_REWIND_PASSWORD=${POSTGRESQL_REWIND_PASSWORD:-rewind123}

      # PostgreSQL Data Directory
      - PGDATA=/var/lib/postgresql/data/pgdata

      # PostgreSQL Databases
      - POSTGRES_DB=${POSTGRES_DB:-postgres}
      - DATALYPTICA_DB=${DATALYPTICA_DB:-datalyptica}
      - NESSIE_DB=${NESSIE_DB:-nessie}
    volumes:
      - patroni1_data:/var/lib/postgresql/data
      - ../secrets/certificates/postgresql:/var/lib/postgresql/certs:ro
      - ../secrets/certificates/ca:/var/lib/postgresql/ca:ro
    secrets:
      - postgres_password
      - datalyptica_password
      - postgres_replication_password
    networks:
      - storage_network
      - data_network
      - control_network
    restart: unless-stopped
    depends_on:
      - etcd-1
      - etcd-2
      - etcd-3
    deploy:
      resources:
        limits:
          cpus: "4.0"
          memory: 4G
        reservations:
          cpus: "2.0"
          memory: 1G
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -h localhost -U postgres || exit 1"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 60s

  # PostgreSQL Patroni Node 2 (Replica - follows primary)
  postgresql-patroni-2:
    image: ghcr.io/datalyptica/datalyptica/patroni:${DATALYPTICA_VERSION:-v1.0.0}
    container_name: ${COMPOSE_PROJECT_NAME}-postgresql-patroni-2
    hostname: postgresql-patroni-2
    ports:
      - "${POSTGRES_PORT_REPLICA:-5433}:5432" # PostgreSQL port
      - "8009:8008" # Patroni REST API
    environment:
      # Patroni Configuration
      - PATRONI_NAME=postgresql-patroni-2
      - PATRONI_SCOPE=lakehouse
      - ETCD_HOSTS=etcd-1:2379,etcd-2:2379,etcd-3:2379

      # Patroni API Authentication
      - PATRONI_API_USERNAME=patroni
      - PATRONI_API_PASSWORD=${PATRONI_API_PASSWORD:-patroni123}

      # PostgreSQL Superuser
      - POSTGRESQL_POSTGRES_PASSWORD_FILE=/run/secrets/postgres_password

      # PostgreSQL Application User
      - POSTGRESQL_USERNAME=${POSTGRES_USER:-datalyptica}
      - POSTGRESQL_PASSWORD_FILE=/run/secrets/datalyptica_password

      # PostgreSQL Replication User
      - POSTGRESQL_REPLICATION_USER=replicator
      - POSTGRESQL_REPLICATION_PASSWORD_FILE=/run/secrets/postgres_replication_password

      # PostgreSQL Rewind User (for pg_rewind)
      - POSTGRESQL_REWIND_USER=rewind_user
      - POSTGRESQL_REWIND_PASSWORD=${POSTGRESQL_REWIND_PASSWORD:-rewind123}

      # PostgreSQL Data Directory
      - PGDATA=/var/lib/postgresql/data/pgdata

      # PostgreSQL Databases
      - POSTGRES_DB=${POSTGRES_DB:-postgres}
      - DATALYPTICA_DB=${DATALYPTICA_DB:-datalyptica}
      - NESSIE_DB=${NESSIE_DB:-nessie}
    volumes:
      - patroni2_data:/var/lib/postgresql/data
      - ../secrets/certificates/postgresql:/var/lib/postgresql/certs:ro
      - ../secrets/certificates/ca:/var/lib/postgresql/ca:ro
    secrets:
      - postgres_password
      - datalyptica_password
      - postgres_replication_password
    networks:
      - storage_network
      - data_network
      - control_network
    restart: unless-stopped
    depends_on:
      - etcd-1
      - etcd-2
      - etcd-3
      - postgresql-patroni-1
    deploy:
      resources:
        limits:
          cpus: "4.0"
          memory: 4G
        reservations:
          cpus: "2.0"
          memory: 1G
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -h localhost -U postgres || exit 1"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 60s

  # HAProxy - PostgreSQL HA Load Balancer with Automatic Failover
  haproxy:
    image: haproxy:2.9-alpine
    container_name: ${COMPOSE_PROJECT_NAME}-haproxy
    hostname: haproxy
    ports:
      - "${HAPROXY_WRITE_PORT:-15000}:5000" # Write endpoint (primary only)
      - "${HAPROXY_READ_PORT:-15001}:5001" # Read endpoint (all nodes)
      - "${HAPROXY_STATS_PORT:-8404}:8404" # Stats dashboard
    volumes:
      - ../../configs/haproxy/haproxy.cfg:/usr/local/etc/haproxy/haproxy.cfg:ro
    networks:
      - storage_network
      - data_network
      - control_network
    restart: unless-stopped
    depends_on:
      - postgresql-patroni-1
      - postgresql-patroni-2
    deploy:
      resources:
        limits:
          cpus: "0.5"
          memory: 256M
        reservations:
          cpus: "0.25"
          memory: 128M
    healthcheck:
      test: ["CMD-SHELL", "timeout 2 nc 127.0.0.1 8404 < /dev/null || exit 1"]
      interval: 10s
      timeout: 5s
      retries: 3
      start_period: 10s

  # Nessie Catalog Service
  nessie:
    image: ghcr.io/datalyptica/datalyptica/nessie:${DATALYPTICA_VERSION:-v1.0.0}
    container_name: ${COMPOSE_PROJECT_NAME}-nessie
    ports:
      - "${NESSIE_PORT}:19120"
      - "${NESSIE_HTTPS_PORT:-19443}:19443"
    env_file:
      - .env.nessie
    environment:
      # Server Configuration
      - QUARKUS_HTTP_PORT=${NESSIE_PORT}
      - QUARKUS_HTTP_HOST=${NESSIE_HOST}
      - QUARKUS_HTTP_SSL_PORT=${NESSIE_HTTPS_PORT:-19443}
      - QUARKUS_HTTP_SSL_CERTIFICATE_FILE=/etc/nessie/certs/server-cert.pem
      - QUARKUS_HTTP_SSL_CERTIFICATE_KEY_FILE=/etc/nessie/certs/server-key.pem
      - QUARKUS_HTTP_INSECURE_REQUESTS=enabled

      # CORS Configuration
      - QUARKUS_HTTP_CORS=${NESSIE_CORS_ENABLED}
      - QUARKUS_HTTP_CORS_ORIGINS=${NESSIE_CORS_ORIGINS}
      - QUARKUS_HTTP_CORS_METHODS=${NESSIE_CORS_METHODS}
      - QUARKUS_HTTP_CORS_HEADERS=${NESSIE_CORS_HEADERS}
      - QUARKUS_HTTP_CORS_EXPOSED_HEADERS=${NESSIE_CORS_EXPOSED_HEADERS}
      - QUARKUS_HTTP_CORS_ACCESS_CONTROL_MAX_AGE=${NESSIE_CORS_MAX_AGE}

      # Database Configuration - Use default datasource (no name)
      - NESSIE_VERSION_STORE_TYPE=${NESSIE_VERSION_STORE_TYPE}

      # Default Quarkus datasource configuration
      - QUARKUS_DATASOURCE_DB_KIND=postgresql
      - QUARKUS_DATASOURCE_JDBC_URL=jdbc:postgresql://${POSTGRES_HOST:-haproxy}:${POSTGRES_PORT:-5000}/nessie
      - QUARKUS_DATASOURCE_USERNAME=nessie
      - QUARKUS_DATASOURCE_PASSWORD=fHx7kgvF9iTQVbOzEpnahelNBMYmqQIQ
      - QUARKUS_DATASOURCE_JDBC_INITIAL_SIZE=${NESSIE_DB_INITIAL_SIZE}
      - QUARKUS_DATASOURCE_JDBC_MIN_SIZE=${NESSIE_DB_MIN_SIZE}
      - QUARKUS_DATASOURCE_JDBC_MAX_SIZE=${NESSIE_DB_MAX_SIZE}

      # PostgreSQL connection for entrypoint health check
      - POSTGRES_HOST=${POSTGRES_HOST:-haproxy}
      - POSTGRES_PORT=${POSTGRES_PORT:-5000}
      - POSTGRES_DB=nessie

      # Catalog Configuration
      - NESSIE_CATALOG_DEFAULT_WAREHOUSE=${NESSIE_DEFAULT_WAREHOUSE}
      - NESSIE_CATALOG_WAREHOUSES_WAREHOUSE_LOCATION=${NESSIE_WAREHOUSE_LOCATION}
      - WAREHOUSE_LOCATION=${WAREHOUSE_LOCATION}

      # Metrics Configuration
      - NESSIE_MICROMETER_ENABLED=${NESSIE_MICROMETER_ENABLED}
      - NESSIE_MICROMETER_EXPORT_PROMETHEUS_ENABLED=${NESSIE_MICROMETER_EXPORT_PROMETHEUS_ENABLED}

      # Health Check Configuration
      - NESSIE_HEALTH_ROOT_PATH=${NESSIE_HEALTH_ROOT_PATH}

      # OpenAPI Configuration
      - NESSIE_SWAGGER_UI_ALWAYS_INCLUDE=${NESSIE_SWAGGER_UI_ALWAYS_INCLUDE}
      - NESSIE_SWAGGER_UI_PATH=${NESSIE_SWAGGER_UI_PATH}

      # Authentication Configuration
      - NESSIE_SERVER_AUTHENTICATION_ENABLED=${NESSIE_SERVER_AUTHENTICATION_ENABLED}

      # S3/MinIO Configuration
      - S3_ACCESS_KEY=${S3_ACCESS_KEY}
      - S3_SECRET_KEY=${S3_SECRET_KEY}
      - S3_ENDPOINT=${S3_ENDPOINT}
      - MINIO_REGION=${MINIO_REGION}
    volumes:
      - ../secrets/certificates/nessie:/etc/nessie/certs:ro
      - ../secrets/certificates/ca:/etc/nessie/ca:ro
    networks:
      - storage_network
      - data_network
    depends_on:
      - haproxy
      - minio
    restart: unless-stopped
    deploy:
      resources:
        limits:
          cpus: "2.0"
          memory: 2G
        reservations:
          cpus: "1.0"
          memory: 512M
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:19120/api/v2/config"]
      interval: ${HEALTHCHECK_INTERVAL}
      timeout: ${HEALTHCHECK_TIMEOUT}
      retries: 5
      start_period: 60s

  # Trino Query Engine
  trino:
    image: ghcr.io/datalyptica/datalyptica/trino:${DATALYPTICA_VERSION:-v1.0.0}
    container_name: ${COMPOSE_PROJECT_NAME}-trino
    ports:
      - "${TRINO_PORT}:8080"
      - "${TRINO_HTTPS_PORT:-8443}:8443"
    environment:
      # Server Configuration
      - TRINO_COORDINATOR=${TRINO_COORDINATOR}
      - TRINO_NODE_SCHEDULER_INCLUDE_COORDINATOR=${TRINO_INCLUDE_COORDINATOR}
      - TRINO_HTTP_SERVER_PORT=${TRINO_PORT}
      - TRINO_HTTPS_PORT=${TRINO_HTTPS_PORT:-8443}
      - TRINO_HTTPS_ENABLED=${TRINO_HTTPS_ENABLED:-true}
      - TRINO_HTTPS_KEYSTORE_PATH=${TRINO_HTTPS_KEYSTORE_PATH:-/etc/trino/certs/server-cert.pem}
      - TRINO_HTTPS_KEYSTORE_KEY=${TRINO_HTTPS_KEYSTORE_KEY:-/etc/trino/certs/server-key.pem}
      - TRINO_DISCOVERY_URI=${TRINO_DISCOVERY_URI}
      - TRINO_QUERY_MAX_MEMORY=${TRINO_QUERY_MAX_MEMORY}
      - TRINO_MEMORY_HEAP_HEADROOM_PER_NODE=${TRINO_MEMORY_HEAP_HEADROOM}

      # Node Configuration
      - TRINO_NODE_ENVIRONMENT=${TRINO_NODE_ENVIRONMENT}
      - TRINO_NODE_DATA_DIR=${TRINO_NODE_DATA_DIR}
      - TRINO_NODE_ID=${TRINO_NODE_ID}

      # JVM Configuration
      - TRINO_JVM_XMX=${TRINO_JVM_XMX}
      - TRINO_JVM_GC=${TRINO_JVM_GC}
      - TRINO_JVM_G1_HEAP_REGION_SIZE=${TRINO_JVM_G1_HEAP_REGION_SIZE}
      - TRINO_JVM_EXIT_ON_OOM=${TRINO_JVM_EXIT_ON_OOM}
      - TRINO_JVM_USE_GC_OVERHEAD_LIMIT=${TRINO_JVM_USE_GC_OVERHEAD_LIMIT}
      - TRINO_JVM_NIO_MAX_CACHED_BUFFER_SIZE=${TRINO_JVM_NIO_MAX_CACHED_BUFFER_SIZE}
      - TRINO_JVM_ALLOW_ATTACH_SELF=${TRINO_JVM_ALLOW_ATTACH_SELF}

      # Logging Configuration
      - TRINO_LOG_LEVEL_ROOT=${TRINO_LOG_LEVEL_ROOT}
      - TRINO_LOG_LEVEL_SERVER=${TRINO_LOG_LEVEL_SERVER}
      - TRINO_LOG_LEVEL_EXECUTION=${TRINO_LOG_LEVEL_EXECUTION}
      - TRINO_LOG_LEVEL_METADATA=${TRINO_LOG_LEVEL_METADATA}
      - TRINO_LOG_LEVEL_SECURITY=${TRINO_LOG_LEVEL_SECURITY}
      - TRINO_LOG_LEVEL_SPI=${TRINO_LOG_LEVEL_SPI}
      - TRINO_LOG_LEVEL_SQL=${TRINO_LOG_LEVEL_SQL}
      - TRINO_LOG_LEVEL_TRANSACTION=${TRINO_LOG_LEVEL_TRANSACTION}
      - TRINO_LOG_LEVEL_TYPE=${TRINO_LOG_LEVEL_TYPE}
      - TRINO_LOG_LEVEL_UTIL=${TRINO_LOG_LEVEL_UTIL}
      - TRINO_LOG_LEVEL_HTTP_REQUEST=${TRINO_LOG_LEVEL_HTTP_REQUEST}

      # Iceberg Catalog Configuration (Nessie-based)
      - TRINO_CATALOG_ICEBERG_CONNECTOR_NAME=icebergoka
      - TRINO_CATALOG_ICEBERG_CATALOG_TYPE=${TRINO_CATALOG_ICEBERG_CATALOG_TYPE}
      - TRINO_CATALOG_ICEBERG_NESSIE_CATALOG_URI=${TRINO_CATALOG_ICEBERG_NESSIE_CATALOG_URI}
      - TRINO_CATALOG_ICEBERG_NESSIE_CATALOG_REF=${TRINO_CATALOG_ICEBERG_NESSIE_CATALOG_REF}
      - TRINO_CATALOG_ICEBERG_NESSIE_CATALOG_DEFAULT_WAREHOUSE_DIR=${TRINO_CATALOG_ICEBERG_NESSIE_CATALOG_DEFAULT_WAREHOUSE_DIR}
      - TRINO_CATALOG_ICEBERG_FILE_FORMAT=${TRINO_CATALOG_ICEBERG_FILE_FORMAT}
      - TRINO_CATALOG_ICEBERG_COMPRESSION_CODEC=${TRINO_CATALOG_ICEBERG_COMPRESSION_CODEC}

      # S3/MinIO Configuration
      - TRINO_CATALOG_ICEBERG_S3_ENDPOINT=${S3_ENDPOINT}
      - TRINO_CATALOG_ICEBERG_S3_REGION=${S3_REGION}
      - TRINO_CATALOG_ICEBERG_S3_PATH_STYLE_ACCESS=${S3_PATH_STYLE_ACCESS}
      - TRINO_CATALOG_ICEBERG_S3_ACCESS_KEY=${S3_ACCESS_KEY}
      - TRINO_CATALOG_ICEBERG_S3_SECRET_KEY=${S3_SECRET_KEY}
      - S3_ENDPOINT=${S3_ENDPOINT}
      - S3_ACCESS_KEY=${S3_ACCESS_KEY}
      - S3_SECRET_KEY=${S3_SECRET_KEY}
      - S3_REGION=${S3_REGION}
      - S3_PATH_STYLE_ACCESS=${S3_PATH_STYLE_ACCESS}
      - FS_NATIVE_S3_ENABLED=true
    volumes:
      - ../secrets/certificates/trino:/etc/trino/certs:ro
      - ../secrets/certificates/ca:/etc/trino/ca:ro
    networks:
      - data_network
      - storage_network
    depends_on:
      minio:
        condition: service_healthy
      nessie:
        condition: service_healthy
    restart: unless-stopped
    deploy:
      resources:
        limits:
          cpus: "4.0"
          memory: 8G
        reservations:
          cpus: "2.0"
          memory: 2G
    healthcheck:
      test: ["CMD", "curl", "-fk", "https://localhost:8443/v1/info"]
      interval: ${HEALTHCHECK_INTERVAL}
      timeout: ${HEALTHCHECK_TIMEOUT}
      retries: ${HEALTHCHECK_RETRIES}
      start_period: 60s

  # Spark Master
  spark-master:
    image: ghcr.io/datalyptica/datalyptica/spark:${DATALYPTICA_VERSION:-v1.0.0}
    container_name: ${COMPOSE_PROJECT_NAME}-spark-master
    ports:
      - "${SPARK_UI_PORT}:4040" # Spark UI
      - "${SPARK_MASTER_PORT}:7077" # Spark Master
    environment:
      # Spark Core Configuration
      - SPARK_MODE=master
      - SPARK_WEBUI_PORT=${SPARK_UI_PORT}
      - SPARK_RPC_AUTHENTICATION_ENABLED=false
      - SPARK_RPC_ENCRYPTION_ENABLED=true
      - SPARK_LOCAL_STORAGE_ENCRYPTION_ENABLED=true
      - SPARK_SSL_ENABLED=true
      - SPARK_SSL_KEYSTORE=/opt/spark/certs/server-cert.pem
      - SPARK_SSL_TRUSTSTORE=/opt/spark/ca/ca-cert.pem
      - SPARK_MASTER_URL=${SPARK_MASTER_URL}
      - SPARK_DRIVER_MEMORY=${SPARK_DRIVER_MEMORY}
      - SPARK_DRIVER_MAX_RESULT_SIZE=${SPARK_DRIVER_MAX_RESULT_SIZE}
      - SPARK_EXECUTOR_MEMORY=${SPARK_EXECUTOR_MEMORY}
      - SPARK_EXECUTOR_CORES=${SPARK_EXECUTOR_CORES}
      - SPARK_EXECUTOR_INSTANCES=${SPARK_EXECUTOR_INSTANCES}
      - SPARK_DYNAMIC_ALLOCATION_ENABLED=${SPARK_DYNAMIC_ALLOCATION_ENABLED}
      - SPARK_SERIALIZER=${SPARK_SERIALIZER}

      # Environment variables for template processing (original approach)
      - NESSIE_URI=${NESSIE_URI}
      - WAREHOUSE_LOCATION=${WAREHOUSE_LOCATION}
      - S3_ENDPOINT=${S3_ENDPOINT}
      - S3_ACCESS_KEY=${S3_ACCESS_KEY}
      - S3_SECRET_KEY=${S3_SECRET_KEY}
      - S3_REGION=${S3_REGION}
      - S3_PATH_STYLE_ACCESS=${S3_PATH_STYLE_ACCESS}
      - MINIO_REGION=${MINIO_REGION}
      # Template processing variables
      - SPARK_ICEBERG_CATALOG_NAME=${SPARK_ICEBERG_CATALOG_NAME}
      - SPARK_ICEBERG_REF=${SPARK_ICEBERG_REF}
      - SPARK_ICEBERG_URI=${SPARK_ICEBERG_URI}
      - SPARK_ICEBERG_WAREHOUSE=${SPARK_ICEBERG_WAREHOUSE}
      - SPARK_ICEBERG_IO_IMPL=${SPARK_ICEBERG_IO_IMPL}
      # AWS Configuration for Iceberg S3FileIO
      - AWS_REGION=${S3_REGION}
      - AWS_ACCESS_KEY_ID=${S3_ACCESS_KEY}
      - AWS_SECRET_ACCESS_KEY=${S3_SECRET_KEY}
      # Spark Configuration Directory
      - SPARK_CONF_DIR=/tmp/spark/conf
    volumes:
      - ../secrets/certificates/spark:/opt/spark/certs:ro
      - ../secrets/certificates/ca:/opt/spark/ca:ro
    networks:
      - data_network
      - storage_network
    depends_on:
      minio:
        condition: service_healthy
      nessie:
        condition: service_healthy
    restart: unless-stopped
    deploy:
      resources:
        limits:
          cpus: "4.0"
          memory: 4G
        reservations:
          cpus: "2.0"
          memory: 1G
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:4040"]
      interval: ${HEALTHCHECK_INTERVAL}
      timeout: ${HEALTHCHECK_TIMEOUT}
      retries: ${HEALTHCHECK_RETRIES}
      start_period: 60s

  # Spark Worker
  spark-worker:
    image: ghcr.io/datalyptica/datalyptica/spark:${DATALYPTICA_VERSION:-v1.0.0}
    container_name: ${COMPOSE_PROJECT_NAME}-spark-worker
    ports:
      - "4041:4040" # Spark Worker UI
    environment:
      # Spark Core Configuration
      - SPARK_MODE=worker
      - SPARK_RPC_AUTHENTICATION_ENABLED=false
      - SPARK_RPC_ENCRYPTION_ENABLED=true
      - SPARK_LOCAL_STORAGE_ENCRYPTION_ENABLED=true
      - SPARK_SSL_ENABLED=true
      - SPARK_SSL_KEYSTORE=/opt/spark/certs/server-cert.pem
      - SPARK_SSL_TRUSTSTORE=/opt/spark/ca/ca-cert.pem
      - SPARK_MASTER_URL=${SPARK_MASTER_URL}
      - SPARK_WORKER_MEMORY=${SPARK_WORKER_MEMORY}
      - SPARK_WORKER_CORES=${SPARK_WORKER_CORES}
      - SPARK_EXECUTOR_MEMORY=${SPARK_EXECUTOR_MEMORY}
      - SPARK_EXECUTOR_CORES=${SPARK_EXECUTOR_CORES}

      # Spark Iceberg Configuration (for Nessie integration)
      - SPARK_ICEBERG_CATALOG_NAME=${SPARK_ICEBERG_CATALOG_NAME}
      - SPARK_ICEBERG_URI=${SPARK_ICEBERG_URI}
      - SPARK_ICEBERG_REF=${SPARK_ICEBERG_REF}
      - SPARK_ICEBERG_WAREHOUSE=${SPARK_ICEBERG_WAREHOUSE}
      - SPARK_ICEBERG_IO_IMPL=${SPARK_ICEBERG_IO_IMPL}

      # Environment variables for template processing
      - NESSIE_URI=${NESSIE_URI}
      - WAREHOUSE_LOCATION=${WAREHOUSE_LOCATION}
      - S3_ENDPOINT=${S3_ENDPOINT}
      - S3_ACCESS_KEY=${S3_ACCESS_KEY}
      - S3_SECRET_KEY=${S3_SECRET_KEY}
      - S3_REGION=${S3_REGION}
      - S3_PATH_STYLE_ACCESS=${S3_PATH_STYLE_ACCESS}
      - MINIO_REGION=${MINIO_REGION}
      # Template processing variables
      - SPARK_ICEBERG_CATALOG_NAME=${SPARK_ICEBERG_CATALOG_NAME}
      - SPARK_ICEBERG_REF=${SPARK_ICEBERG_REF}
      - SPARK_ICEBERG_URI=${SPARK_ICEBERG_URI}
      - SPARK_ICEBERG_WAREHOUSE=${SPARK_ICEBERG_WAREHOUSE}
      - SPARK_ICEBERG_IO_IMPL=${SPARK_ICEBERG_IO_IMPL}
      # AWS Configuration for Iceberg S3FileIO
      - AWS_REGION=${S3_REGION}
      - AWS_ACCESS_KEY_ID=${S3_ACCESS_KEY}
      - AWS_SECRET_ACCESS_KEY=${S3_SECRET_KEY}
      # Spark Configuration Directory
      - SPARK_CONF_DIR=/tmp/spark/conf
    volumes:
      - ../secrets/certificates/spark:/opt/spark/certs:ro
      - ../secrets/certificates/ca:/opt/spark/ca:ro
    networks:
      - data_network
      - storage_network
      - control_network
    depends_on:
      spark-master:
        condition: service_healthy
    restart: unless-stopped
    deploy:
      resources:
        limits:
          cpus: "4.0"
          memory: 4G
        reservations:
          cpus: "2.0"
          memory: 1G
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:4040"]
      interval: ${HEALTHCHECK_INTERVAL}
      timeout: ${HEALTHCHECK_TIMEOUT}
      retries: ${HEALTHCHECK_RETRIES}
      start_period: 60s

  # Apache Kafka Cluster (KRaft Mode - No ZooKeeper Required)
  # Kafka Broker 1
  kafka-1:
    image: ghcr.io/datalyptica/datalyptica/kafka:${DATALYPTICA_VERSION:-v1.0.0}
    container_name: ${COMPOSE_PROJECT_NAME}-kafka-1
    hostname: kafka-1
    ports:
      - "${KAFKA_PORT:-9092}:9092"
      - "9093:9093"
      - "9094:9094" # Controller port for KRaft
    environment:
      # KRaft Mode Configuration
      - KAFKA_PROCESS_ROLES=broker,controller
      - KAFKA_NODE_ID=1
      - KAFKA_CONTROLLER_QUORUM_VOTERS=1@kafka-1:9094,2@kafka-2:9094,3@kafka-3:9094
      - KAFKA_CONTROLLER_LISTENER_NAMES=CONTROLLER
      - CLUSTER_ID=xR9VObP1SvO9P4tYW3VApw

      # Listeners Configuration (with SSL/TLS)
      - KAFKA_LISTENERS=PLAINTEXT://0.0.0.0:9092,SSL://0.0.0.0:9093,CONTROLLER://0.0.0.0:9094
      - KAFKA_ADVERTISED_LISTENERS=PLAINTEXT://kafka-1:9092,SSL://localhost:9093
      - KAFKA_LISTENER_SECURITY_PROTOCOL_MAP=PLAINTEXT:PLAINTEXT,SSL:SSL,CONTROLLER:PLAINTEXT
      - KAFKA_INTER_BROKER_LISTENER_NAME=PLAINTEXT
      - KAFKA_SSL_KEYSTORE_LOCATION=/etc/kafka/certs/server-cert.pem
      - KAFKA_SSL_KEYSTORE_FILENAME=server-cert.pem
      - KAFKA_SSL_KEYSTORE_TYPE=PEM
      - KAFKA_SSL_KEY_LOCATION=/etc/kafka/certs/server-key.pem
      - KAFKA_SSL_TRUSTSTORE_LOCATION=/etc/kafka/ca/ca-cert.pem
      - KAFKA_SSL_TRUSTSTORE_TYPE=PEM
      - KAFKA_SSL_CLIENT_AUTH=none

      # Replication & Topic Configuration (HA Settings)
      - KAFKA_OFFSETS_TOPIC_REPLICATION_FACTOR=3
      - KAFKA_TRANSACTION_STATE_LOG_REPLICATION_FACTOR=3
      - KAFKA_TRANSACTION_STATE_LOG_MIN_ISR=2
      - KAFKA_DEFAULT_REPLICATION_FACTOR=3
      - KAFKA_MIN_INSYNC_REPLICAS=2
      - KAFKA_AUTO_CREATE_TOPICS_ENABLE=true
      - KAFKA_LOG_RETENTION_HOURS=168
      - KAFKA_LOG_SEGMENT_BYTES=1073741824
      - KAFKA_NUM_PARTITIONS=3

      # Storage Configuration
      - KAFKA_LOG_DIRS=/var/lib/kafka/data
    volumes:
      - kafka_1_data:/var/lib/kafka/data
      - ../secrets/certificates/kafka:/etc/kafka/certs:ro
      - ../secrets/certificates/ca:/etc/kafka/ca:ro
    networks:
      - control_network
      - data_network
    restart: unless-stopped
    deploy:
      resources:
        limits:
          cpus: "2.0"
          memory: 2G
        reservations:
          cpus: "1.0"
          memory: 512M
    healthcheck:
      test:
        [
          "CMD",
          "kafka-broker-api-versions",
          "--bootstrap-server",
          "localhost:9092",
        ]
      interval: 10s
      timeout: 10s
      retries: 5
      start_period: 40s

  # Kafka Broker 2
  kafka-2:
    image: ghcr.io/datalyptica/datalyptica/kafka:${DATALYPTICA_VERSION:-v1.0.0}
    container_name: ${COMPOSE_PROJECT_NAME}-kafka-2
    hostname: kafka-2
    ports:
      - "9192:9092" # External port for broker 2
      - "9193:9093"
      - "9194:9094"
    environment:
      # KRaft Mode Configuration
      - KAFKA_PROCESS_ROLES=broker,controller
      - KAFKA_NODE_ID=2
      - KAFKA_CONTROLLER_QUORUM_VOTERS=1@kafka-1:9094,2@kafka-2:9094,3@kafka-3:9094
      - KAFKA_CONTROLLER_LISTENER_NAMES=CONTROLLER
      - CLUSTER_ID=xR9VObP1SvO9P4tYW3VApw

      # Listeners Configuration
      - KAFKA_LISTENERS=PLAINTEXT://0.0.0.0:9092,SSL://0.0.0.0:9093,CONTROLLER://0.0.0.0:9094
      - KAFKA_ADVERTISED_LISTENERS=PLAINTEXT://kafka-2:9092,SSL://localhost:9193
      - KAFKA_LISTENER_SECURITY_PROTOCOL_MAP=PLAINTEXT:PLAINTEXT,SSL:SSL,CONTROLLER:PLAINTEXT
      - KAFKA_INTER_BROKER_LISTENER_NAME=PLAINTEXT
      - KAFKA_SSL_KEYSTORE_LOCATION=/etc/kafka/certs/server-cert.pem
      - KAFKA_SSL_KEYSTORE_TYPE=PEM
      - KAFKA_SSL_KEY_LOCATION=/etc/kafka/certs/server-key.pem
      - KAFKA_SSL_TRUSTSTORE_LOCATION=/etc/kafka/ca/ca-cert.pem
      - KAFKA_SSL_TRUSTSTORE_TYPE=PEM
      - KAFKA_SSL_CLIENT_AUTH=none

      # Replication & Topic Configuration
      - KAFKA_OFFSETS_TOPIC_REPLICATION_FACTOR=3
      - KAFKA_TRANSACTION_STATE_LOG_REPLICATION_FACTOR=3
      - KAFKA_TRANSACTION_STATE_LOG_MIN_ISR=2
      - KAFKA_DEFAULT_REPLICATION_FACTOR=3
      - KAFKA_MIN_INSYNC_REPLICAS=2
      - KAFKA_AUTO_CREATE_TOPICS_ENABLE=true
      - KAFKA_LOG_RETENTION_HOURS=168
      - KAFKA_LOG_SEGMENT_BYTES=1073741824
      - KAFKA_NUM_PARTITIONS=3

      # Storage Configuration
      - KAFKA_LOG_DIRS=/var/lib/kafka/data
    volumes:
      - kafka_2_data:/var/lib/kafka/data
      - ../secrets/certificates/kafka:/etc/kafka/certs:ro
      - ../secrets/certificates/ca:/etc/kafka/ca:ro
    networks:
      - control_network
      - data_network
    restart: unless-stopped
    deploy:
      resources:
        limits:
          cpus: "2.0"
          memory: 2G
        reservations:
          cpus: "1.0"
          memory: 512M
    healthcheck:
      test:
        [
          "CMD",
          "kafka-broker-api-versions",
          "--bootstrap-server",
          "localhost:9092",
        ]
      interval: 10s
      timeout: 10s
      retries: 5
      start_period: 40s

  # Kafka Broker 3
  kafka-3:
    image: ghcr.io/datalyptica/datalyptica/kafka:${DATALYPTICA_VERSION:-v1.0.0}
    container_name: ${COMPOSE_PROJECT_NAME}-kafka-3
    hostname: kafka-3
    ports:
      - "9292:9092" # External port for broker 3
      - "9293:9093"
      - "9294:9094"
    environment:
      # KRaft Mode Configuration
      - KAFKA_PROCESS_ROLES=broker,controller
      - KAFKA_NODE_ID=3
      - KAFKA_CONTROLLER_QUORUM_VOTERS=1@kafka-1:9094,2@kafka-2:9094,3@kafka-3:9094
      - KAFKA_CONTROLLER_LISTENER_NAMES=CONTROLLER
      - CLUSTER_ID=xR9VObP1SvO9P4tYW3VApw

      # Listeners Configuration
      - KAFKA_LISTENERS=PLAINTEXT://0.0.0.0:9092,SSL://0.0.0.0:9093,CONTROLLER://0.0.0.0:9094
      - KAFKA_ADVERTISED_LISTENERS=PLAINTEXT://kafka-3:9092,SSL://localhost:9293
      - KAFKA_LISTENER_SECURITY_PROTOCOL_MAP=PLAINTEXT:PLAINTEXT,SSL:SSL,CONTROLLER:PLAINTEXT
      - KAFKA_INTER_BROKER_LISTENER_NAME=PLAINTEXT
      - KAFKA_SSL_KEYSTORE_LOCATION=/etc/kafka/certs/server-cert.pem
      - KAFKA_SSL_KEYSTORE_TYPE=PEM
      - KAFKA_SSL_KEY_LOCATION=/etc/kafka/certs/server-key.pem
      - KAFKA_SSL_TRUSTSTORE_LOCATION=/etc/kafka/ca/ca-cert.pem
      - KAFKA_SSL_TRUSTSTORE_TYPE=PEM
      - KAFKA_SSL_CLIENT_AUTH=none

      # Replication & Topic Configuration
      - KAFKA_OFFSETS_TOPIC_REPLICATION_FACTOR=3
      - KAFKA_TRANSACTION_STATE_LOG_REPLICATION_FACTOR=3
      - KAFKA_TRANSACTION_STATE_LOG_MIN_ISR=2
      - KAFKA_DEFAULT_REPLICATION_FACTOR=3
      - KAFKA_MIN_INSYNC_REPLICAS=2
      - KAFKA_AUTO_CREATE_TOPICS_ENABLE=true
      - KAFKA_LOG_RETENTION_HOURS=168
      - KAFKA_LOG_SEGMENT_BYTES=1073741824
      - KAFKA_NUM_PARTITIONS=3

      # Storage Configuration
      - KAFKA_LOG_DIRS=/var/lib/kafka/data
    volumes:
      - kafka_3_data:/var/lib/kafka/data
      - ../secrets/certificates/kafka:/etc/kafka/certs:ro
      - ../secrets/certificates/ca:/etc/kafka/ca:ro
    networks:
      - control_network
      - data_network
    restart: unless-stopped
    deploy:
      resources:
        limits:
          cpus: "2.0"
          memory: 2G
        reservations:
          cpus: "1.0"
          memory: 512M
    healthcheck:
      test:
        [
          "CMD",
          "kafka-broker-api-versions",
          "--bootstrap-server",
          "localhost:9092",
        ]
      interval: 10s
      timeout: 10s
      retries: 5
      start_period: 40s

  # Confluent Schema Registry
  schema-registry:
    image: ghcr.io/datalyptica/datalyptica/schema-registry:${DATALYPTICA_VERSION:-v1.0.0}
    container_name: ${COMPOSE_PROJECT_NAME}-schema-registry
    ports:
      - "${SCHEMA_REGISTRY_PORT:-8085}:8081"
      - "${SCHEMA_REGISTRY_HTTPS_PORT:-8445}:8443"
    environment:
      - SCHEMA_REGISTRY_HOST_NAME=schema-registry
      - SCHEMA_REGISTRY_KAFKASTORE_BOOTSTRAP_SERVERS=kafka-1:9092,kafka-2:9092,kafka-3:9092
      - SCHEMA_REGISTRY_LISTENERS=http://0.0.0.0:8081,https://0.0.0.0:8443
      - SCHEMA_REGISTRY_KAFKASTORE_TOPIC=_schemas
      - SCHEMA_REGISTRY_KAFKASTORE_TOPIC_REPLICATION_FACTOR=1
      - SCHEMA_REGISTRY_DEBUG=false
      - SCHEMA_REGISTRY_AVRO_COMPATIBILITY_LEVEL=BACKWARD
      - SCHEMA_REGISTRY_LOG4J_ROOT_LOGLEVEL=INFO
      - SCHEMA_REGISTRY_SSL_KEYSTORE_LOCATION=/etc/schema-registry/certs/server-cert.pem
      - SCHEMA_REGISTRY_SSL_KEYSTORE_PASSWORD=""
      - SCHEMA_REGISTRY_SSL_KEY_PASSWORD=""
      - SCHEMA_REGISTRY_SSL_TRUSTSTORE_LOCATION=/etc/schema-registry/ca/ca-cert.pem
    volumes:
      - schema_registry_data:/var/lib/schema-registry
      - ../secrets/certificates/schema-registry:/etc/schema-registry/certs:ro
      - ../secrets/certificates/ca:/etc/schema-registry/ca:ro
    networks:
      - control_network
    depends_on:
      kafka-1:
        condition: service_healthy
      kafka-2:
        condition: service_healthy
      kafka-3:
        condition: service_healthy
    restart: unless-stopped
    deploy:
      resources:
        limits:
          cpus: "1.0"
          memory: 1G
        reservations:
          cpus: "0.5"
          memory: 256M
    healthcheck:
      test: ["CMD", "/usr/local/bin/healthcheck.sh"]
      interval: 10s
      timeout: 5s
      retries: 3
      start_period: 30s

  # Kafka UI (Management Interface)
  kafka-ui:
    image: provectuslabs/kafka-ui:${KAFKA_UI_VERSION:-v0.7.2}
    container_name: ${COMPOSE_PROJECT_NAME}-kafka-ui
    ports:
      - "${KAFKA_UI_PORT:-8090}:8080"
    environment:
      - KAFKA_CLUSTERS_0_NAME=datalyptica-cluster
      - KAFKA_CLUSTERS_0_BOOTSTRAPSERVERS=kafka-1:9092,kafka-2:9092,kafka-3:9092
      - KAFKA_CLUSTERS_0_ZOOKEEPER=zookeeper:2181
      - KAFKA_CLUSTERS_0_METRICS_PORT=9997
      - KAFKA_CLUSTERS_0_SCHEMAREGISTRY=http://schema-registry:8081
    networks:
      - management_network
      - control_network
    depends_on:
      kafka-1:
        condition: service_healthy
      kafka-2:
        condition: service_healthy
      kafka-3:
        condition: service_healthy
      schema-registry:
        condition: service_healthy
    restart: unless-stopped
    deploy:
      resources:
        limits:
          cpus: "1.0"
          memory: 512M
        reservations:
          cpus: "0.25"
          memory: 128M
    healthcheck:
      test:
        [
          "CMD-SHELL",
          "wget --no-verbose --tries=1 --spider http://localhost:8080/actuator/health || exit 1",
        ]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 30s

  # Flink JobManager
  flink-jobmanager:
    image: ghcr.io/datalyptica/datalyptica/flink:${DATALYPTICA_VERSION:-v1.0.0}
    container_name: ${COMPOSE_PROJECT_NAME}-flink-jobmanager
    ports:
      - "${FLINK_JOBMANAGER_PORT:-8081}:8081"
      - "6123:6123"
    command: jobmanager
    environment:
      JOB_MANAGER_RPC_ADDRESS: flink-jobmanager
      KAFKA_BOOTSTRAP_SERVERS: kafka-1:9092,kafka-2:9092,kafka-3:9092
      NESSIE_URI: ${NESSIE_URI:-http://nessie:19120/api/v2}
      WAREHOUSE_LOCATION: ${WAREHOUSE_LOCATION:-s3a://lakehouse/warehouse}
      S3_ENDPOINT: ${S3_ENDPOINT:-http://minio:9000}
      S3_ACCESS_KEY: ${S3_ACCESS_KEY}
      S3_SECRET_KEY: ${S3_SECRET_KEY}
      S3_PATH_STYLE_ACCESS: "true"
      FLINK_SSL_ENABLED: "true"
      FLINK_SSL_KEYSTORE: "/opt/flink/certs/server-cert.pem"
      FLINK_SSL_TRUSTSTORE: "/opt/flink/ca/ca-cert.pem"
    volumes:
      - flink_checkpoints:/opt/flink/checkpoints
      - flink_savepoints:/opt/flink/savepoints
      - ../secrets/certificates/flink:/opt/flink/certs:ro
      - ../secrets/certificates/ca:/opt/flink/ca:ro
    networks:
      - data_network
      - control_network
    depends_on:
      kafka-1:
        condition: service_healthy
      kafka-2:
        condition: service_healthy
      kafka-3:
        condition: service_healthy
      nessie:
        condition: service_healthy
      minio:
        condition: service_healthy
    restart: unless-stopped
    deploy:
      resources:
        limits:
          cpus: "2.0"
          memory: 2G
        reservations:
          cpus: "1.0"
          memory: 512M
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8081/overview"]
      interval: 10s
      timeout: 5s
      retries: 3
      start_period: 30s

  # Flink TaskManager
  flink-taskmanager:
    image: ghcr.io/datalyptica/datalyptica/flink:${DATALYPTICA_VERSION:-v1.0.0}
    container_name: ${COMPOSE_PROJECT_NAME}-flink-taskmanager
    depends_on:
      flink-jobmanager:
        condition: service_healthy
    command: taskmanager
    environment:
      JOB_MANAGER_RPC_ADDRESS: flink-jobmanager
      TASK_MANAGER_NUMBER_OF_TASK_SLOTS: ${FLINK_TASK_SLOTS:-4}
      KAFKA_BOOTSTRAP_SERVERS: kafka-1:9092,kafka-2:9092,kafka-3:9092
      NESSIE_URI: ${NESSIE_URI:-http://nessie:19120/api/v2}
      WAREHOUSE_LOCATION: ${WAREHOUSE_LOCATION:-s3a://lakehouse/warehouse}
      S3_ENDPOINT: ${S3_ENDPOINT:-http://minio:9000}
      S3_ACCESS_KEY: ${S3_ACCESS_KEY}
      S3_SECRET_KEY: ${S3_SECRET_KEY}
      S3_PATH_STYLE_ACCESS: "true"
      FLINK_SSL_ENABLED: "true"
      FLINK_SSL_KEYSTORE: "/opt/flink/certs/server-cert.pem"
      FLINK_SSL_TRUSTSTORE: "/opt/flink/ca/ca-cert.pem"
    volumes:
      - flink_checkpoints:/opt/flink/checkpoints
      - flink_savepoints:/opt/flink/savepoints
      - ../secrets/certificates/flink:/opt/flink/certs:ro
      - ../secrets/certificates/ca:/opt/flink/ca:ro
    networks:
      - data_network
      - control_network
    restart: unless-stopped
    deploy:
      resources:
        limits:
          cpus: "2.0"
          memory: 2G
        reservations:
          cpus: "1.0"
          memory: 512M
    healthcheck:
      test: ["CMD", "curl", "-f", "http://flink-jobmanager:8081/taskmanagers"]
      interval: 10s
      timeout: 5s
      retries: 3
      start_period: 30s

  # Kafka Connect with Debezium (Avro Support)
  kafka-connect:
    image: ghcr.io/datalyptica/datalyptica/kafka-connect:${DATALYPTICA_VERSION:-v1.0.0}
    container_name: ${COMPOSE_PROJECT_NAME}-kafka-connect
    ports:
      - "${KAFKA_CONNECT_PORT:-8083}:8083"
      - "${KAFKA_CONNECT_HTTPS_PORT:-8444}:8444"
    environment:
      - CONNECT_BOOTSTRAP_SERVERS=kafka-1:9092,kafka-2:9092,kafka-3:9092
      - CONNECT_REST_PORT=8083
      - CONNECT_REST_ADVERTISED_PORT=8083
      - CONNECT_REST_ADVERTISED_HTTPS_PORT=8444
      - CONNECT_LISTENERS=http://0.0.0.0:8083,https://0.0.0.0:8444
      - CONNECT_GROUP_ID=debezium-connect-cluster
      - CONNECT_CONFIG_STORAGE_TOPIC=debezium-connect-configs
      - CONNECT_OFFSET_STORAGE_TOPIC=debezium-connect-offsets
      - CONNECT_STATUS_STORAGE_TOPIC=debezium-connect-status
      - CONNECT_CONFIG_STORAGE_REPLICATION_FACTOR=1
      - CONNECT_OFFSET_STORAGE_REPLICATION_FACTOR=1
      - CONNECT_STATUS_STORAGE_REPLICATION_FACTOR=1
      # Avro Converters with Schema Registry
      - CONNECT_KEY_CONVERTER=io.confluent.connect.avro.AvroConverter
      - CONNECT_KEY_CONVERTER_SCHEMA_REGISTRY_URL=http://schema-registry:8081
      - CONNECT_VALUE_CONVERTER=io.confluent.connect.avro.AvroConverter
      - CONNECT_VALUE_CONVERTER_SCHEMA_REGISTRY_URL=http://schema-registry:8081
      - CONNECT_INTERNAL_KEY_CONVERTER=org.apache.kafka.connect.json.JsonConverter
      - CONNECT_INTERNAL_VALUE_CONVERTER=org.apache.kafka.connect.json.JsonConverter
      - CONNECT_REST_ADVERTISED_HOST_NAME=kafka-connect
      - CONNECT_PLUGIN_PATH=/usr/share/confluent-hub-components
      # SSL/TLS Configuration
      - CONNECT_SSL_KEYSTORE_LOCATION=/etc/kafka-connect/certs/server-cert.pem
      - CONNECT_SSL_TRUSTSTORE_LOCATION=/etc/kafka-connect/ca/ca-cert.pem
    volumes:
      - kafka_connect_data:/var/lib/kafka-connect
      - ../secrets/certificates/kafka-connect:/etc/kafka-connect/certs:ro
      - ../secrets/certificates/ca:/etc/kafka-connect/ca:ro
    networks:
      - data_network
      - control_network
      - storage_network
    depends_on:
      kafka-1:
        condition: service_healthy
      kafka-2:
        condition: service_healthy
      kafka-3:
        condition: service_healthy
      schema-registry:
        condition: service_healthy
      postgresql-patroni-1:
        condition: service_healthy
    restart: unless-stopped
    deploy:
      resources:
        limits:
          cpus: "2.0"
          memory: 2G
        reservations:
          cpus: "1.0"
          memory: 512M
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8083/"]
      interval: 10s
      timeout: 5s
      retries: 3
      start_period: 30s

  # ClickHouse (OLAP database)
  clickhouse:
    image: ghcr.io/datalyptica/datalyptica/clickhouse:${DATALYPTICA_VERSION:-v1.0.0}
    container_name: ${COMPOSE_PROJECT_NAME}-clickhouse
    ports:
      - "${CLICKHOUSE_HTTP_PORT:-8123}:8123"
      - "${CLICKHOUSE_HTTPS_PORT:-8443}:8443"
      - "${CLICKHOUSE_NATIVE_PORT:-9000}:9000"
      - "${CLICKHOUSE_NATIVE_SECURE_PORT:-9440}:9440"
      - "9009:9009"
    environment:
      - CLICKHOUSE_DB=${CLICKHOUSE_DB:-default}
      - CLICKHOUSE_USER=${CLICKHOUSE_USER:-default}
      - CLICKHOUSE_PASSWORD_FILE=/run/secrets/clickhouse_password
      - CLICKHOUSE_DEFAULT_ACCESS_MANAGEMENT=1
      - CLICKHOUSE_HTTPS_PORT=${CLICKHOUSE_HTTPS_PORT:-8443}
      - CLICKHOUSE_TCP_PORT_SECURE=${CLICKHOUSE_NATIVE_SECURE_PORT:-9440}
      - CLICKHOUSE_OPENSSL_SERVER_CERT_FILE=/etc/clickhouse-server/certs/server-cert.pem
      - CLICKHOUSE_OPENSSL_SERVER_KEY_FILE=/etc/clickhouse-server/certs/server-key.pem
      - CLICKHOUSE_OPENSSL_CA_CERT_FILE=/etc/clickhouse-server/ca/ca-cert.pem
    volumes:
      - clickhouse_data:/var/lib/clickhouse
      - clickhouse_logs:/var/log/clickhouse-server
      - ../secrets/certificates/clickhouse:/etc/clickhouse-server/certs:ro
      - ../secrets/certificates/ca:/etc/clickhouse-server/ca:ro
    secrets:
      - clickhouse_password
    networks:
      - storage_network
      - data_network
    depends_on:
      kafka-1:
        condition: service_healthy
      kafka-2:
        condition: service_healthy
      kafka-3:
        condition: service_healthy
    restart: unless-stopped
    deploy:
      resources:
        limits:
          cpus: "4.0"
          memory: 4G
        reservations:
          cpus: "2.0"
          memory: 1G
    healthcheck:
      test: ["CMD", "clickhouse-client", "--query", "SELECT 1"]
      interval: 10s
      timeout: 5s
      retries: 3
      start_period: 30s

  # dbt for SQL Transformations
  dbt:
    image: ghcr.io/datalyptica/datalyptica/dbt:${DATALYPTICA_VERSION:-v1.0.0}
    container_name: ${COMPOSE_PROJECT_NAME}-dbt
    ports:
      - "${DBT_DOCS_PORT:-8580}:8580"
    environment:
      - TRINO_HOST=trino
      - TRINO_PORT=8080
      - SPARK_HOST=spark-master
      - SPARK_PORT=10000
      - DBT_PROFILES_DIR=/root/.dbt
    volumes:
      - dbt_project:/dbt
      - dbt_profiles:/root/.dbt
    networks:
      - data_network
      - storage_network
    depends_on:
      trino:
        condition: service_healthy
      spark-master:
        condition: service_healthy
    restart: unless-stopped
    deploy:
      resources:
        limits:
          cpus: "1.0"
          memory: 1G
        reservations:
          cpus: "0.5"
          memory: 256M
    command: ["sh", "-c", "sleep infinity"]

  # Prometheus Monitoring
  prometheus:
    image: prom/prometheus:${PROMETHEUS_VERSION:-v2.48.0}
    container_name: ${COMPOSE_PROJECT_NAME}-prometheus
    ports:
      - "${PROMETHEUS_PORT:-9090}:9090"
      - "${PROMETHEUS_HTTPS_PORT:-9443}:9443"
    environment:
      - TZ=${TIMEZONE:-UTC}
    volumes:
      - ../../configs/prometheus/prometheus.yml:/etc/prometheus/prometheus.yml:ro
      - ../../configs/prometheus/alerts.yml:/etc/prometheus/alerts.yml:ro
      - ../secrets/certificates/prometheus:/etc/prometheus/certs:ro
      - ../secrets/certificates/ca:/etc/prometheus/ca:ro
      - prometheus_data:/prometheus
    command:
      - "--config.file=/etc/prometheus/prometheus.yml"
      - "--storage.tsdb.path=/prometheus"
      - "--storage.tsdb.retention.time=${PROMETHEUS_RETENTION_TIME:-15d}"
      - "--web.console.libraries=/usr/share/prometheus/console_libraries"
      - "--web.console.templates=/usr/share/prometheus/consoles"
      - "--web.enable-lifecycle"
      - "--web.listen-address=:9443"
      - "--web.config.file=/etc/prometheus/web-config.yml"
    networks:
      - management_network
      - data_network
      - storage_network
      - control_network
    restart: unless-stopped
    deploy:
      resources:
        limits:
          cpus: "2.0"
          memory: 2G
        reservations:
          cpus: "1.0"
          memory: 512M
    healthcheck:
      test:
        [
          "CMD",
          "wget",
          "--quiet",
          "--tries=1",
          "--spider",
          "--no-check-certificate",
          "https://localhost:9443/-/healthy",
        ]
      interval: ${HEALTHCHECK_INTERVAL:-30s}
      timeout: ${HEALTHCHECK_TIMEOUT:-10s}
      retries: ${HEALTHCHECK_RETRIES:-3}
      start_period: ${HEALTHCHECK_START_PERIOD:-60s}

  # Grafana Dashboards
  grafana:
    image: grafana/grafana:${GRAFANA_VERSION:-10.2.2}
    container_name: ${COMPOSE_PROJECT_NAME}-grafana
    ports:
      - "${GRAFANA_PORT:-3000}:3000"
      - "${GRAFANA_HTTPS_PORT:-3443}:3443"
    environment:
      - GF_SECURITY_ADMIN_USER=${GRAFANA_ADMIN_USER:-admin}
      - GF_SECURITY_ADMIN_PASSWORD__FILE=/run/secrets/grafana_admin_password
      - GF_USERS_ALLOW_SIGN_UP=${GRAFANA_ALLOW_SIGN_UP:-false}
      - GF_SERVER_ROOT_URL=${GRAFANA_ROOT_URL:-https://localhost:3443}
      - GF_SERVER_PROTOCOL=${GRAFANA_PROTOCOL:-https}
      - GF_SERVER_HTTP_PORT=${GRAFANA_PORT:-3000}
      - GF_SERVER_HTTPS_PORT=${GRAFANA_HTTPS_PORT:-3443}
      - GF_SERVER_CERT_FILE=/etc/grafana/certs/server-cert.pem
      - GF_SERVER_CERT_KEY=/etc/grafana/certs/server-key.pem
      - GF_INSTALL_PLUGINS=${GRAFANA_INSTALL_PLUGINS:-}
      - TZ=${TIMEZONE:-UTC}
    volumes:
      - grafana_data:/var/lib/grafana
      - ../../configs/grafana/provisioning:/etc/grafana/provisioning:ro
      - ../secrets/certificates/grafana:/etc/grafana/certs:ro
      - ../secrets/certificates/ca:/etc/grafana/ca:ro
    secrets:
      - grafana_admin_password
    networks:
      - management_network
    depends_on:
      prometheus:
        condition: service_healthy
    restart: unless-stopped
    deploy:
      resources:
        limits:
          cpus: "1.0"
          memory: 1G
        reservations:
          cpus: "0.5"
          memory: 256M
    healthcheck:
      test:
        [
          "CMD",
          "wget",
          "--quiet",
          "--tries=1",
          "--spider",
          "--no-check-certificate",
          "https://localhost:3443/api/health",
        ]
      interval: ${HEALTHCHECK_INTERVAL:-30s}
      timeout: ${HEALTHCHECK_TIMEOUT:-10s}
      retries: ${HEALTHCHECK_RETRIES:-3}
      start_period: ${HEALTHCHECK_START_PERIOD:-60s}

  # Loki Log Aggregation
  loki:
    image: grafana/loki:${LOKI_VERSION:-2.9.3}
    container_name: ${COMPOSE_PROJECT_NAME}-loki
    ports:
      - "${LOKI_PORT:-3100}:3100"
      - "${LOKI_HTTPS_PORT:-3143}:3143"
    environment:
      - TZ=${TIMEZONE:-UTC}
    volumes:
      - ../../configs/loki/loki-config.yml:/etc/loki/local-config.yaml:ro
      - ../secrets/certificates/loki:/etc/loki/certs:ro
      - ../secrets/certificates/ca:/etc/loki/ca:ro
      - loki_data:/loki
    command: -config.file=/etc/loki/local-config.yaml
    networks:
      - management_network
    restart: unless-stopped
    deploy:
      resources:
        limits:
          cpus: "2.0"
          memory: 2G
        reservations:
          cpus: "0.5"
          memory: 512M
    healthcheck:
      test:
        [
          "CMD",
          "wget",
          "--quiet",
          "--tries=1",
          "--spider",
          "http://localhost:3100/ready",
        ]
      interval: ${HEALTHCHECK_INTERVAL:-30s}
      timeout: ${HEALTHCHECK_TIMEOUT:-10s}
      retries: ${HEALTHCHECK_RETRIES:-3}
      start_period: ${HEALTHCHECK_START_PERIOD:-60s}

  # Grafana Alloy Log Collector (Promtail successor)
  alloy:
    image: grafana/alloy:${ALLOY_VERSION:-v1.0.0}
    container_name: ${COMPOSE_PROJECT_NAME}-alloy
    volumes:
      - ../../configs/loki/alloy-config.alloy:/etc/alloy/config.alloy:ro
      - /var/run/docker.sock:/var/run/docker.sock:ro
      - /var/lib/docker/containers:/var/lib/docker/containers:ro
      - alloy_data:/var/lib/alloy
    command:
      - run
      - /etc/alloy/config.alloy
      - --server.http.listen-addr=0.0.0.0:12345
      - --storage.path=/var/lib/alloy
    ports:
      - "${ALLOY_HTTP_PORT:-12345}:12345" # Alloy HTTP API
    networks:
      - management_network
      - data_network
      - storage_network
      - control_network
    depends_on:
      loki:
        condition: service_healthy
      prometheus:
        condition: service_healthy
    restart: unless-stopped
    deploy:
      resources:
        limits:
          cpus: "1.0"
          memory: 1G
        reservations:
          cpus: "0.5"
          memory: 256M
    healthcheck:
      test: ["CMD-SHELL", "pgrep -f 'alloy.*run' || exit 1"]
      interval: ${HEALTHCHECK_INTERVAL:-30s}
      timeout: ${HEALTHCHECK_TIMEOUT:-10s}
      retries: ${HEALTHCHECK_RETRIES:-3}
      start_period: ${HEALTHCHECK_START_PERIOD:-30s}

  # Alertmanager for Alert Routing and Notifications
  alertmanager:
    image: prom/alertmanager:${ALERTMANAGER_VERSION:-v0.27.0}
    container_name: ${COMPOSE_PROJECT_NAME}-alertmanager
    ports:
      - "${ALERTMANAGER_PORT:-9095}:9093"
      - "${ALERTMANAGER_HTTPS_PORT:-9445}:9445"
    environment:
      - TZ=${TIMEZONE:-UTC}
    volumes:
      - ../../configs/alertmanager/alertmanager.yml:/etc/alertmanager/alertmanager.yml:ro
      - ../../configs/alertmanager/templates:/etc/alertmanager/templates:ro
      - ../secrets/certificates/alertmanager:/etc/alertmanager/certs:ro
      - ../secrets/certificates/ca:/etc/alertmanager/ca:ro
      - alertmanager_data:/alertmanager
    command:
      - "--config.file=/etc/alertmanager/alertmanager.yml"
      - "--storage.path=/alertmanager"
      - "--web.external-url=https://localhost:9445"
      - "--web.listen-address=:9445"
      - "--cluster.advertise-address=0.0.0.0:9445"
      - "--web.config.file=/etc/alertmanager/web-config.yml"
    networks:
      - management_network
    depends_on:
      prometheus:
        condition: service_healthy
    restart: unless-stopped
    deploy:
      resources:
        limits:
          cpus: "0.5"
          memory: 512M
        reservations:
          cpus: "0.25"
          memory: 128M
    healthcheck:
      test:
        [
          "CMD",
          "wget",
          "--quiet",
          "--tries=1",
          "--spider",
          "http://localhost:9093/-/healthy",
        ]
      interval: ${HEALTHCHECK_INTERVAL:-30s}
      timeout: ${HEALTHCHECK_TIMEOUT:-10s}
      retries: ${HEALTHCHECK_RETRIES:-3}
      start_period: ${HEALTHCHECK_START_PERIOD:-30s}

  # Keycloak Identity and Access Management
  keycloak:
    image: quay.io/keycloak/keycloak:${KEYCLOAK_VERSION:-23.0}
    container_name: ${COMPOSE_PROJECT_NAME}-keycloak
    ports:
      - "${KEYCLOAK_PORT:-8180}:8080"
      - "${KEYCLOAK_HTTPS_PORT:-8543}:8443"
    entrypoint: ["/bin/bash", "-c"]
    command:
      - |
        export KC_DB_PASSWORD=$$(cat /run/secrets/datalyptica_password)
        export KEYCLOAK_ADMIN_PASSWORD=$$(cat /run/secrets/keycloak_admin_password)
        /opt/keycloak/bin/kc.sh start-dev
    environment:
      # Database Configuration
      - KC_DB=postgres
      - KC_DB_URL=jdbc:postgresql://haproxy:5000/${KEYCLOAK_DB:-keycloak}
      - KC_DB_USERNAME=${KEYCLOAK_DB_USER:-keycloak}

      # Admin User
      - KEYCLOAK_ADMIN=${KEYCLOAK_ADMIN_USER:-admin}

      # Keycloak Configuration
      - KC_HOSTNAME=${KEYCLOAK_HOSTNAME:-localhost}
      - KC_HOSTNAME_PORT=${KEYCLOAK_HTTPS_PORT:-8543}
      - KC_HOSTNAME_STRICT=false
      - KC_HOSTNAME_STRICT_HTTPS=false
      - KC_HTTP_ENABLED=true
      - KC_HTTPS_ENABLED=true
      - KC_HTTPS_CERTIFICATE_FILE=/opt/keycloak/certs/server-cert.pem
      - KC_HTTPS_CERTIFICATE_KEY_FILE=/opt/keycloak/certs/server-key.pem
      - KC_HEALTH_ENABLED=true
      - KC_METRICS_ENABLED=true

      # Proxy Configuration
      - KC_PROXY=edge
      - KC_HOSTNAME_STRICT_BACKCHANNEL=false
    volumes:
      - keycloak_data:/opt/keycloak/data
      - ../secrets/certificates/keycloak:/opt/keycloak/certs:ro
      - ../secrets/certificates/ca:/opt/keycloak/ca:ro
    secrets:
      - keycloak_admin_password
      - datalyptica_password
    networks:
      - management_network
      - storage_network
      - data_network
    depends_on:
      - haproxy
    restart: unless-stopped
    deploy:
      resources:
        limits:
          cpus: "2.0"
          memory: 2G
        reservations:
          cpus: "0.5"
          memory: 512M
    healthcheck:
      test:
        [
          "CMD-SHELL",
          "exec 3<>/dev/tcp/localhost/8080 && echo -e 'GET /health/ready HTTP/1.1\\r\\nHost: localhost\\r\\nConnection: close\\r\\n\\r\\n' >&3 && cat <&3 | grep -q '200\\|UP' || exit 1",
        ]
      interval: 30s
      timeout: 10s
      retries: 5
      start_period: 60s

volumes:
  minio_data:
    driver: local
  # etcd cluster volumes (3 nodes for HA)
  etcd1_data:
    driver: local
  etcd2_data:
    driver: local
  etcd3_data:
    driver: local
  # PostgreSQL Patroni volumes (2 nodes for HA)
  patroni1_data:
    driver: local
  patroni2_data:
    driver: local
  # Legacy volume (kept for backward compatibility)
  postgresql_data:
    driver: local
  schema_registry_data:
    driver: local
  kafka_connect_data:
    driver: local
  flink_checkpoints:
    driver: local
  flink_savepoints:
    driver: local
  clickhouse_data:
    driver: local
  clickhouse_logs:
    driver: local
  kafka_1_data:
    driver: local
  kafka_2_data:
    driver: local
  kafka_3_data:
    driver: local
  dbt_project:
    driver: local
  dbt_profiles:
    driver: local
  prometheus_data:
    driver: local
  grafana_data:
    driver: local
  loki_data:
    driver: local
  alloy_data:
    driver: local
  alertmanager_data:
    driver: local
  keycloak_data:
    driver: local

networks:
  # Management Network - Monitoring and observability services
  management_network:
    name: ${COMPOSE_PROJECT_NAME}_management
    driver: bridge
    ipam:
      config:
        - subnet: 172.20.0.0/16

  # Control Network - Orchestration and coordination services
  control_network:
    name: ${COMPOSE_PROJECT_NAME}_control
    driver: bridge
    ipam:
      config:
        - subnet: 172.21.0.0/16

  # Data Network - Processing engines and compute services
  data_network:
    name: ${COMPOSE_PROJECT_NAME}_data
    driver: bridge
    ipam:
      config:
        - subnet: 172.22.0.0/16

  # Storage Network - Data persistence and storage services
  storage_network:
    name: ${COMPOSE_PROJECT_NAME}_storage
    driver: bridge
    ipam:
      config:
        - subnet: 172.23.0.0/16
