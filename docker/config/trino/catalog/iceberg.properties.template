connector.name=iceberg
iceberg.catalog.type=nessie
iceberg.nessie-catalog.uri=${TRINO_CATALOG_ICEBERG_NESSIE_CATALOG_URI}
iceberg.nessie-catalog.default-warehouse-dir=${TRINO_CATALOG_ICEBERG_NESSIE_CATALOG_DEFAULT_WAREHOUSE_DIR}
iceberg.nessie-catalog.ref=${TRINO_CATALOG_ICEBERG_NESSIE_CATALOG_REF}
iceberg.file-format=${TRINO_CATALOG_ICEBERG_FILE_FORMAT}
iceberg.compression-codec=${TRINO_CATALOG_ICEBERG_COMPRESSION_CODEC}
s3.endpoint=${TRINO_CATALOG_ICEBERG_S3_ENDPOINT}
s3.region=${TRINO_CATALOG_ICEBERG_S3_REGION}
s3.aws-access-key=${TRINO_CATALOG_ICEBERG_S3_ACCESS_KEY}
s3.aws-secret-key=${TRINO_CATALOG_ICEBERG_S3_SECRET_KEY}
s3.path-style-access=${TRINO_CATALOG_ICEBERG_S3_PATH_STYLE_ACCESS}
