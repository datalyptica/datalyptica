server:
  host: "0.0.0.0"
  port: 8080
  read_timeout: 30
  write_timeout: 30
  environment: "development"

logger:
  level: "info"
  environment: "development"
  time_format: "15:04:05"

docker:
  compose_file: "docker-compose.yml"
  project_name: "shudl"
  default_registry: ""
  timeout: 300
  environment:
    # Add any environment variables for Docker commands
    # DOCKER_HOST: "unix:///var/run/docker.sock" 