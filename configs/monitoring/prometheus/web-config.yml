tls_server_config:
  cert_file: /etc/prometheus/certs/server-cert.pem
  key_file: /etc/prometheus/certs/server-key.pem
  client_ca_file: /etc/prometheus/ca/ca-cert.pem
  client_auth_type: RequestClientCert

